import { useState } from "react";
export function SubmitButton() {
  // WHEN an answer is selected THEN the submit button will appear

  // WHEN the submit button is clicked THEN the correct choice will be revealed
  // WHEN the answer is shown THEN the next question will be created
  // WHEN the next question appears THEN the submit button will disappear UNTIL a new anser is selected

  return (
    <>
      <button></button>
    </>
  );
}
